import { TransformFieldPipe } from './transform-field.pipe';

describe('TransformFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
